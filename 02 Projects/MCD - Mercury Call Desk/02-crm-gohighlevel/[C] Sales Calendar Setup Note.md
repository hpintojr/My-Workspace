---
type: setup
date: 2026-06-25
project: MCD - Mercury Call Desk
status: Action plan — Hamilton to execute; Claude can't access the Google/GHL accounts directly.
---

# Sales / Demo Calendar Setup

Right now GHL is linked to **hpintojr@gmail**. Goal: move scheduling to a dedicated business account so it's clean, separable from personal, and reassignable later.

## Recommended approach
1. **Create the business Google account.** Either a new Gmail (e.g., `mcd.scheduling@gmail.com`) or, better long-term, **Google Workspace** on `mercurycalldesk.com` so the address is `scheduling@mercurycalldesk.com` (matches your support@/sales@). Workspace also gives you a real business calendar to share.
2. **In GHL → Settings → My Profile / Calendar Settings:** disconnect the `hpintojr@gmail` calendar integration, then connect the new business Google account.
3. **Create two calendars in GHL:**
   - **Sales Call** (discovery/intro) — round-robin to you for now; later assign to agents.
   - **Demo** (with company support) — you/service rep as host, agent as guest.
4. **Grab the booking links** for each and add them to `[C] Owner Setup & Open Decisions.md` (sales calendar link + demo calendar link), then into the email/text templates (`collateral/`).
5. **Per-agent later:** when you build enclosed environments with your AE, give each agent their own calendar/booking link inside their environment.

## Notes
- Migrating from a personal to a business account avoids tangling MCD bookings with your personal calendar and makes it easy to hand off.
- Confirm time zone (Pacific) and buffer/availability rules on each calendar.
- Once links exist, tell me and I'll drop them into the templates and Owner Setup automatically.
