---
author: claude
type: daily
date: 2026-07-03
---

# Session Log — Friday, July 3 2026

## What We Worked On

- MCD CRM - Agent and Admin Portals — resolved a stuck GitHub Desktop merge on `hpintojr/My-Workspace` (a stale `.git/index.lock` had frozen the repo mid-conflict across 6 files).
- MCD CRM - Agent and Admin Portals — ran an independent, read-only 13-layer production rescope of `crm.mcd` (repo, Vercel, Neon) after Hamilton flagged he wasn't satisfied with ChatGPT's version and wanted a second, verified pass before deciding anything further.
- Workspace process — established a new hard rule: this workspace repo is edited only through the GitHub connector/MCP going forward, never through the local `D:\GitHub\My Workspace` folder.

## What Was Built or Changed

- Fixed the GitHub Desktop merge: removed the stale lock file, restored the stash, reconciled `00 [C] Workspace Index.md`, `CLAUDE.md`, and the MCD CRM Overview doc against GitHub's authoritative content, and pushed a clean merge commit (`59b171f8`) — verified via the GitHub API that `origin/main`'s tip matched exactly afterward.
- New file: `02 Projects/MCD CRM - Agent and Admin Portals/[C] Independent 13-Layer Production Rescope.md` — full layer-by-layer assessment built from direct evidence (PR #30's actual 9-file diff, the real repo tree, Vercel deployment/build-log/env data, and live Neon role/RLS queries), not from prior notes.
- Key correction in that rescope: the specific claim that a `clientAccountId` vs `id` route collision explains PR #30's preview 504 doesn't hold up — that same collision already exists on `main` at the commit currently live in production, and production works. Root cause of the login/MFA hang is still open.
- Confirmed directly (not inferred) during the rescope: RLS is off on all 21 tables in Neon and the only login-capable role (`neondb_owner`) bypasses RLS by default; preview and production currently share the same `LEAD_IMPORT_HMAC_SECRET`/`LEAD_IMPORT_KEY_ID`; zero GitHub Actions workflows exist; Neon compute is fixed at 0.25 CU with no autoscaling headroom.
- Updated `CLAUDE.md` with a "Tooling rule — 2026-07-04" section (GitHub MCP only, no local folder writes) and a pointer to the new rescope doc.

## Still Open

- PR #30's login/MFA 504 root cause is unresolved — next step is reproducing it live with browser devtools and real-time Vercel logs open, not assuming any single fix clears it.
- Database privilege fix (least-privilege runtime role, RLS decision) and HMAC secret separation between preview/production are both unaddressed and, per the rescope, shouldn't wait for the login bug to be solved first.
- Backlog items #38–41 (recurring source config, admin visibility, duplicate-dispatch prevention, company/entity metadata) remain not yet scoped with Hamilton.
- Four open questions for Hamilton sit at the end of the rescope doc (pilot scope, whether to fix the DB role this week regardless, release-approval owner, timeline trade-offs).

## Start Here Tomorrow

Read `02 Projects/MCD CRM - Agent and Admin Portals/[C] Independent 13-Layer Production Rescope.md` for Hamilton's answers to its four closing questions, then move on whichever of the login-bug reproduction or the database-role fix he wants prioritized first — both are independent of each other and can start immediately.
